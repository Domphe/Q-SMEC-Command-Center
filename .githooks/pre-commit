#!/usr/bin/env bash
# Canonical Q-SMEC pre-commit hook chain.
# AUTO-GENERATED by Q-SMEC-Claude/hooks/propagate-canonical-pre-commit.sh.
# Manual edits will be overwritten on next propagator run.
#
# Chain (in order):
#   1. Repo-local custom checks (if .githooks/pre-commit.local exists)
#   2. bash-syntax    -- block .sh parse errors
#   3. R72 no-estimates -- block approximate/estimated count phrasings
#   4. R57 preflight  -- block large delete/move ops without preflight
#
# References:
#   Q-SMEC-Claude/CLAUDE.md §R57 + §R72
#   Q-SMEC-Claude/shared-memory/protocol_folder_migration_preflight.md
#   Q-SMEC-Claude/shared-memory/feedback_no_estimates_definitive_counts_only.md
set -e

# Repo-local custom checks (e.g. gotchas-index drift in Q-SMEC-Claude)
if [[ -x .githooks/pre-commit.local ]]; then
  .githooks/pre-commit.local "$@"
fi

/mnt/e/Data1/Q-SMEC-Claude/hooks/pre-commit-bash-syntax.sh "$@"
/mnt/e/Data1/Q-SMEC-Claude/hooks/pre-commit-no-estimates.sh "$@"
/mnt/e/Data1/Q-SMEC-Claude/hooks/pre-commit-obsidian-coverage.sh "$@"
exec /mnt/e/Data1/Q-SMEC-Claude/hooks/pre-commit-r57-check.sh "$@"
